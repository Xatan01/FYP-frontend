import { useWindowDimensions } from "react-native";
import { scale, verticalScale, moderateScale } from "react-native-size-matters";
export { scale, verticalScale, moderateScale, useWindowDimensions };
